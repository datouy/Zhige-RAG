# ============================================================
# 开发与测试依赖
# ============================================================
#     pip install -r requirements-core.txt -r requirements-dev.txt
# ============================================================

pytest>=8.0.0
pytest-cov>=4.1.0
ruff>=0.6.0          # CI 使用的 linter（.github/workflows/ci.yml）
